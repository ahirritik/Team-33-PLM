namespace PLM.Models
{
    public enum ProductStatus
    {
        Active,
        Archived
    }

    public enum BoMStatus
    {
        Active,
        Archived
    }

    public enum ECOType
    {
        Product,
        BoM
    }

    public enum ECOStage
    {
        New,
        Approval,
        Done
    }
}
